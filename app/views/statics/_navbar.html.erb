      <nav class="navbar navbar-expand-lg navbar-dark bg-info shadow">
        <div class="container">
          <%= link_to "Prins Solution", root_path, class: "navbar-brand"%>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar-primary" aria-controls="navbar-primary" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbar-primary">
            <div class="navbar-collapse-header">
              <div class="row">
                <div class="col-6 collapse-close">
                  <button type="button" class="navbar-toggler" data-toggle="collapse" data-target="#navbar-primary" aria-controls="navbar-primary" aria-expanded="false" aria-label="Toggle navigation">
                    <span></span>
                    <span></span>
                  </button>
                </div>
              </div>
            </div>
            <ul class="navbar-nav ml-lg-auto">
              <li class="nav-item">
                <%= link_to "View", registers_path, class: "nav-link"%>
              </li>
              <li class="nav-item">
                <%= link_to "New", new_register_path, class: "nav-link"%>
              </li>
              <% if logged_in? %>
                <% if current_user.admin? %>
                <li class="nav-item">
                  <%= link_to 'Admin', rails_admin_path, class: "nav-link" %>
                </li>
                <% end %>
              <li class="nav-item">
                <%= link_to 'Logout', session_path('logout'), method: :delete, class: "nav-link" %>
              </li>
              <% else %>
              <li class="nav-item">
                <a class="nav-link" data-toggle="modal" data-target="#modal-form">Login</a>
              </li>     
              <% end %>
            </ul>
          </div>
        </div>
      </nav>