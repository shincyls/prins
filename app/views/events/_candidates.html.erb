<% @poll.poll_candidates.each do |pc| %>
    <%= button_to pc.full_desc, vote_poll_path(id: @poll.id, voter_id: session[:evote_id], can_id: pc.id), method: "post", remote: true, class: "btn btn-outline-primary w-100 m-2 #{pc.voted_by(session[:evote_id])}" %>
<% end %>