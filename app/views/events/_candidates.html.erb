<% @poll.poll_candidates.each do |pc| %>
    <%= button_to pc.description, vote_poll_path(id: @poll.id, can_id: pc.id), method: "post", remote: true, class: "btn btn-outline-primary w-100 mt-2 #{pc.voted_by(session[:evote_id])}" %>
<% end %>